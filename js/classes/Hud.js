FlappyPlane.Hud = function(game) {
    this.game = game;
};

// Setup the example
FlappyPlane.Hud.prototype.create = function() {
};

// The update() method is called every frame
FlappyPlane.Hud.prototype.update = function() {
};